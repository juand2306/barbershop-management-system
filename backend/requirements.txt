# ─────────────────────────────────────────────────────────────
# requirements.txt — Dependencias base (dev + prod)
# Para produccion tambien instalar: requirements-prod.txt
# ─────────────────────────────────────────────────────────────

# Core Django
Django>=4.2,<5.0
djangorestframework>=3.14
django-cors-headers>=4.0
djangorestframework-simplejwt>=5.2
python-dotenv>=1.0

# Filtros y busqueda
django-filter>=23.0

# Tareas asincronas
celery>=5.3
django-celery-beat>=2.5
redis>=4.5

# Servidor de produccion
gunicorn>=20.1

# Base de datos PostgreSQL (produccion)
psycopg2-binary>=2.9

# Archivos estaticos en produccion (sin necesidad de nginx)
whitenoise>=6.5

# Parseo de DATABASE_URL que inyecta Railway
dj-database-url>=2.0

# Cloudinary (almacenamiento de fotos)
cloudinary>=1.36.0
